let mongoose,Schema;_914‍.x([["Message",()=>Message]]);_914‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}],["Schema",["Schema"],function(v){Schema=v}]]);
const { ObjectId } = Schema
const MessageSchema = new Schema({
  senderId: {
    type: ObjectId,
    ref: 'User',
  },
  receiverId: {
    type: ObjectId,
    ref: 'User',
  },
  sentAt: {
    type: Date,
    default: Date.now,
  },
  text: String,
  read: { type: Boolean, default: false },
  readAt: Date,
})

// middleware

       const Message = mongoose.model('Message', MessageSchema)
